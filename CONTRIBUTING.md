# Contributing

Use the commands in [README.md](README.md) to build and test. Include a regression test for backend behavior changes and exercise cluster operations only against disposable OpenSearch instances. Preserve the original MIT license notice and attribution.

Run `npm ci` and `npm run lint` before submitting changes. The lint command checks AngularJS source with ESLint and trailing whitespace in project text files.
